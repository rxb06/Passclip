# Passclip dependencies

# Required
rich>=13.0
cryptography>=46.0.7   # AES-256-GCM vault export/import; floor = current patched line

# Recommended — clipboard support (auto-clear after timeout)
pyperclip

# Optional — TOTP/OTP code generation
pyotp
